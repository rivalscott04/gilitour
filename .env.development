# Loaded only for `bun run dev`. Overrides root `.env` for this mode.
# Leave VITE_API_BASE_URL unset → http://localhost:8000/api/v1 (see src/lib/api-client.ts).
# VITE_API_BASE_URL=http://127.0.0.1:8000/api/v1
