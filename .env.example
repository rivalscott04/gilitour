# Copy to `.env` for secrets or overrides shared across modes (optional).
# Mode-specific defaults live in `.env.development` and `.env.production`.
#
# Optional override (wins over dev/prod defaults in api-client):
# VITE_API_BASE_URL=https://api.example.com/api/v1
