# Loaded only for `bun run build`. Overrides root `.env` for production bundles.
# Leave VITE_API_BASE_URL unset → same-origin `/api/v1` (reverse-proxy to Laravel).
# VITE_API_BASE_URL=https://api.example.com/api/v1
